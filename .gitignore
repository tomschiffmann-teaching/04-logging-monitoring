# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Generated by the logging exercise (03_formatting_and_file.py writes this)
app.log
*.log

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Docker / monitoring ---
# (the stacks keep Prometheus/Grafana data inside containers, not on disk,
#  but ignore these in case anyone adds bind-mounted volumes later)
prometheus-data/
grafana-data/
*.env.local
# holds personal SMTP credentials for the optional email-alerting task
.env
